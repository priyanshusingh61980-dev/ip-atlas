# Novatrix IP — environment template. Copy to .env.local and fill in values.
# Currently the app runs against the mock AI service; no real keys required.

# Future: VITE_AI_PROVIDER=mock | openai | azure
VITE_AI_PROVIDER=mock
